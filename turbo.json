{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "PORT",
    "VITE_WS_URL",
    "VITE_DEV_SERVER_URL",
    "ELECTRON_RENDERER_PORT",
    "FATMA_LOG_WS_EVENTS",
    "FATMA_AUTO_BOOTSTRAP_PROJECT_FROM_CWD",
    "FATMA_MODE",
    "FATMA_HOST",
    "FATMA_PORT",
    "FATMA_NO_BROWSER",
    "FATMA_STATE_DIR",
    "FATMA_AUTH_TOKEN",
    "FATMA_DESKTOP_WS_URL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "dist-electron/**"]
    },
    "dev": {
      "dependsOn": ["@fatma/contracts#build"],
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": [],
      "cache": false
    },
    "test": {
      "dependsOn": ["^build"],
      "cache": false,
      "outputs": []
    }
  }
}
